# Natural_Language_Processing-Spam_Classification
Here we are using a dataset which includes emojis, punctuations, (mainly spam and ham messages), we are using the logistic regression model to classify whether the message is spam or not. As the dataset is unbalanced we are using boosting model with 20 logistic regression model with learning rate as 1
